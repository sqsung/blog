---
title: "How does DNS work under the hood?"
summary: "Core concepts you need to understand for properly deploying your website to the world wide web."
isPublished: true
tags: ["DNS", "Network", "CS"]
createdAt: "2025-07-05"
---

# DNS Concepts

This is your actual blog post content. You can use all the standard Markdown syntax here.

## What is DNS?

DNS stands for Domain Name System...

### Key Points

- DNS translates domain names to IP addresses
- It's like the phonebook of the internet
- Uses a hierarchical structure

You can also use JSX components if you want:

## Code Examples

<div>hi</div>

```typescript
export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${sourceCodePro.variable} ${inter.variable} antialiased`}
      >
        <ThemeProvider>
          <div className="flex flex-col items-center">
            <div className="flex w-full max-w-[1000px] flex-col gap-10">
              <Header />
              {children}
              <Footer />
            </div>
          </div>
        </ThemeProvider>
      </body>
    </html>
  );
}
```
